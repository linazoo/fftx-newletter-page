import FutureFrontLogo from "../fftxlogo.png";

export const Nav = () => {
  return (
    <nav>
      <img
        className="logo"
        src={FutureFrontLogo}
        alt="future front texas logo"
      />
    </nav>
  );
};
